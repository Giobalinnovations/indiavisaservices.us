export const metadata = {
  title: 'Cancel',
  description: 'Cancel',
  alternates: {
    canonical: '/cancel',
  },
};

export default function CancelLayout({ children }) {
  return <>{children}</>;
}
